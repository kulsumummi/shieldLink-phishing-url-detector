services:
  - type: web
    name: shieldlink-phishing-detector
    env: python
    buildCommand: pip install -r requirements.txt && python model/train_model.py
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
